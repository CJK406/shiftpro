

// import Modal from './ModalComponent'

export default {
  install: (/*app*/) => {
    // app.component('Modal', Modal)
    // app.config.globalProperties.$modal = app.component('Modal')
  
  }
}

